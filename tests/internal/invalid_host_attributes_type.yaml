tags:
  - web:
      description: 'web server'
  - master_db:
      description: 'master database'
  - slave_db:
      description: 'slave database'
  - memcached:
      description: 'memcached'
hosts:
  - localhost: [ '127.0.0.1' ] # not hash -> error
